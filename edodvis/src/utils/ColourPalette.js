// not complete yet but can just copy the entire thing into your code for now!
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      dark: "#748AA1",
      main: "#FFFFF",
      contrastText: "#fff",
    },
    background: {
      default: "#E6ECF2",
    },
    text: {
      primary: "#31394D",
    },
  },
});
